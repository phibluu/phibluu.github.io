export default function Title(props) {
    return(
        <div className="relative flex items-center justify-center py-[1.5vh]">
            <button className="bg-stone-400/20 backdrop-blur-xs py-[1vh] px-[3vh] 
            rounded-full text-3xl hover:scale-120 text-white transition duration-700 ease-(buttons)">
                portfolio.dev
            </button>
        </div>
    )
}